package controllers;

import models.Game;
import models.State;

public class AcceptController extends Controller {

    public AcceptController(Game game, State state) {
        super(game, state);
    }
}